# Release 3.0

Marketing version 3.0.0, minimum iOS 27. The TestFlight build number is the
`BUILD` of the `tf-3.0.0-BUILD` tag that requested it.

3.0 is a full redesign and a simplification, built around the question the app
exists to answer: what is the current region's alert status? The phone has two
tabs, Status and Details; the region always follows location; CarPlay has two
tabs, Status and Alert map. The major version marks the scope of the change, not
a single feature. The map is a reference image, never a navigation surface.

## Changes since 2.9

| Area | Change | Commit |
|---|---|---|
| Status | Redesigned screen; nearby alerts stay visible during an active alert | `5d0e05b` |
| Region | Location only: no Regions tab, search, follow switch or manual pin; Kyiv without location access, with a prompt to enable it; the map opens a read-only region list; a session's first fix commits at once | `8922be3`, `aa4b339` |
| Details | New tab: full summary, Live Activity switch, Restore Purchases and Manage Subscription, data source, disclaimer; replaces the About screen | `0cbe4a0` |
| CarPlay | Two tabs, Status and Alert map; the data source is a Status row | `589846a`, `07bdda0` |
| CarPlay | Alert map tab: the map as a reference image, a region-count row, the affected list, Refresh on demand; the image loads only on opening the tab and on Refresh, never on a timer | `d48b6a6` |
| iPhone map | Inline on the Status tab in a box that reserves the raster's shape; loaded once per session after the status request, never on a timer and not by pull to refresh | `c5bd99a` |
| Widgets, Live Activity | Redesigned; a stale alert keeps "Alert" in red and only a confirmed quiet status downgrades it | `4e87e90` |
| Onboarding | Real first-launch onboarding; the location prompt waits until it is finished; an outside-Ukraine sheet that triggers on leaving the country | `757fb10`, `405a2a0`, `a9c52df` |
| Pro | No Pro surface is presented in 3.0.x (ADR 0014); entitlement, restore and renewal handling stay in the app | `6cc2603`..`7d35ffa` |
| Refresh | Ten-second fetch floor serving the held snapshot; escalating backoff after HTTP 429 | `6c28245`, `930c0c9` |
| Launch | Launch mark and cold start: a cached status appears immediately rather than behind a checking animation | `1493281`, `8247623`, `6bd5ac0` |
| Icon | New Mark icon with Dark and Tinted appearances | `6b37b5f`, `23825b7` |
| Platform | Minimum iOS 27 | `196989f` |
| Localization | Every key in all three catalogs carries en, ru and uk, enforced by a test; Shortcuts no longer shows raw identifiers in ru and uk | `742dba2`, `49ccee2` |

Engineering changes with no user-visible effect — snapshot determinism, the
screenshot tooling, CI and build gating, file splits — are recorded in
[lessons.md](../../lessons.md) and in the redesign brief.

## What's New

The store text says nothing about formerly paid features (owner, 2026-09-20).
The owner compares these three texts with what is saved in App Store Connect
before submitting.

### English

```
Drive Check 3.0 is simpler, and built around one question: is your region under
alert?

• Two tabs: Status and Details
• Status shows your region at a glance, a nearby-alert line, and the alert map
  right under it
• Your region always follows your location — nothing to pick or pin. Tap the
  map to see every region's status
• CarPlay: Status and an Alert map tab, refreshed on demand
• Refreshed widgets and Live Activity: once a region is under alert it stays
  red until a real all-clear arrives
• Faster, steadier launch

Drive Check answers one question — it is not navigation, and it does not track
you in the background.
```

### Ukrainian

```
Drive Check 3.0 став простішим і відповідає на одне питання: чи є тривога у
вашому регіоні?

• Дві вкладки: «Статус» і «Деталі»
• «Статус» показує ваш регіон з одного погляду, рядок про тривоги поруч і
  карту тривог одразу під ним
• Регіон завжди визначається за вашим місцезнаходженням — нічого не треба
  обирати. Торкніться карти, щоб побачити статус усіх регіонів
• CarPlay: «Статус» і вкладка з картою тривог, оновлення на вимогу
• Оновлені віджети та Live Activity: регіон лишається червоним, доки не
  надійде справжній відбій
• Швидший і стабільніший запуск

Drive Check відповідає на одне питання — це не навігація, і застосунок не
відстежує вас у фоні.
```

### Russian

```
Drive Check 3.0 стал проще и отвечает на один вопрос: есть ли тревога в вашем
регионе?

• Две вкладки: «Статус» и «Детали»
• «Статус» показывает ваш регион с одного взгляда, строку о тревогах рядом и
  карту тревог сразу под ним
• Регион всегда определяется по вашему местоположению — ничего не нужно
  выбирать. Коснитесь карты, чтобы увидеть статус всех регионов
• CarPlay: «Статус» и вкладка с картой тревог, обновление по запросу
• Обновлённые виджеты и Live Activity: регион остаётся красным, пока не придёт
  настоящий отбой
• Более быстрый и стабильный запуск

Drive Check отвечает на один вопрос — это не навигация, и приложение не
отслеживает вас в фоне.
```

### Promotional text (≤170)

| Locale | Text |
|---|---|
| en | Your region's alert status at a glance on CarPlay and iPhone. Simpler in 3.0: two tabs, the alert map on the main screen, and a region that follows your location. |
| uk | Статус тривоги у вашому регіоні з одного погляду в CarPlay та на iPhone. У 3.0 простіше: дві вкладки, карта тривог на головному екрані, регіон за місцезнаходженням. |
| ru | Статус тревоги в вашем регионе с одного взгляда в CarPlay и на iPhone. В 3.0 проще: две вкладки, карта тревог на главном экране, регион по местоположению. |

## Validation and delivery

Authoritative order and detail:
[redesign-3.0-owner-handoff.md](../redesign-3.0-owner-handoff.md). This section
summarises it and does not compete with it.

1. The release-prep commit carries this note and the `[3.0]` section of
   `CHANGELOG.md`; `MARKETING_VERSION` is already `3.0.0` in every target and
   configuration. Run `just verify` and `just release --check`.
2. Mark the pre-redesign 3.0.0 candidate "do not submit" in App Store Connect
   if it is still submittable; the new build's number must land above it.
3. Push the release-prep commit, then tag it `tf-3.0.0-N` and push the tag.
4. Wait for the build to reach the Friends&Family group in TestFlight.
5. Run the manual pass — section 2 of the owner hand-off.
6. Upload the English screenshot set captured after the last redesign change,
   reviewed by drivecheck-product first.
7. Select the build, paste the What's New copy above and the review notes
   below, and submit.
8. After submission, tag the submitted commit `v3.0.0`.

## App Review notes

```
Drive Check is a CarPlay and iPhone utility for regional alert status —
informational only, not a background monitor, and not navigation.

The alert map, on CarPlay and on iPhone, is a static reference image fetched
over HTTPS from the public Ubilling endpoint. It is not interactive, not
navigable, and not fetched on a timer: on demand when the map is opened, on manual Refresh, and when its
day/night image variant changes.

No account, no sign-in.

In-app purchases: the subscription products regioncheck.pro.monthly and
regioncheck.pro.yearly remain in App Store Connect, but version 3.0 presents no
purchase screen and gates nothing behind them. Existing subscribers keep working
renewals; Restore Purchases, and Manage Subscription while a subscription is
active, are on the Details tab. The session Live Activity ends when the phone
backgrounds unless CarPlay is connected.

Location: when-in-use only, with an on-device reverse geocode to resolve the
region.

Alert persistence, which a reviewer testing on an intermittent connection will
notice: once a region shows Alert it stays Alert through transient refresh
failures and stale data. Only a confirmed all-clear from the provider
downgrades it. This is deliberate — the app must not imply safety it cannot
confirm.
```
