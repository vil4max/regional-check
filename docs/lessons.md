# Lessons

Record a lesson only when a failure changed a spec, a requirement, a decision,
the core, or agent instructions. Name the check that now catches it.

| Date | Failure | Layer changed | Check that now catches it | Replay |
|---|---|---|---|---|
| 2026-09-18 | Snapshot baselines could not be reproduced anywhere: nothing pinned the run's environment, so a Mac at UTC+3 and the UTC runner rendered different times in every baseline that prints one, and two previews raced the stencil's settle delay (a live spinner, an async image load). The CI `Snapshot tests` job reported success throughout, because its step is `continue-on-error` and it silently records a baseline that is missing | L2 checks: `TestPlans/Snapshots.xctestplan` pins `TZ=UTC`, language `en` and region `US` (4da1f53); the two racing previews are static via DEBUG-only seams (5362dd9); `docs/engineering/testing-strategy.md` states that a baseline recorded against an unpinned plan is not evidence on CI | The pinned plan, plus the integrator's pre-landing check that a branch touching a `.prefire.yml` source lands its baselines | Unset `TZ` in the plan, re-record a time-showing baseline on a non-UTC Mac, and read the CI job's log rather than its conclusion |
| 2026-09-18 | Every App Store screenshot showed the stale-data banner: `StatusController.applyScreenshotFixture` hardcoded `Date(timeIntervalSince1970: 1_720_000_000)` (2024-07-03) instead of the controller's injected clock, so the capture advertised the app failing at its one job | L2 check: `controller_appliesScreenshotFixture_usesInjectedClockNotAFrozenLiteral` (a67b619) asserts the fixture uses the injected clock; fix in 290f3fb | That test | Put a literal back into `applyScreenshotFixture` and run `just test` |
| 2026-09-18 | The App Store capture script waited a fixed 2.5 s after launch, so under build-slot contention it photographed the launch screen and a half-transitioned tab bar, and reported success | L2 checks: `wait_for_stable_frame` polls for two identical probe frames (54d5ad4) and a phase that never settles warns per phase and again in the exit summary (f9bcff3, ad803bf) | The poll plus the loud cap warning — an unconfirmed capture is now flagged, not silently returned | Replace the poll with a fixed `sleep` and run a capture while two builds hold the slots |
| 2026-09-17 | Two Xcode Cloud workflows, two tag namespaces and two promotion scripts existed for one artifact: "App Store candidate (release tag)" was identical to the TestFlight workflow in Actions and Post-actions, so a release built the same commit a second time | L1 `decisions/0013-one-build-pipeline-or-two.md` (supersedes the `release` rows of ADR 0010 and ADR 0012); `docs/operations/release-process.md` invariants 1-4 and 6 | `release.yml` promotes nothing and `check-release-tag.sh` refuses a `v` tag whose commit has no `tf-` tag of that version, so a release tag cannot name a commit that was never built | Tag a commit `vX.Y.Z` without a `tf-X.Y.Z-N` tag on it and confirm "Release marker" fails and no build starts |
| 2026-09-17 | Two `CarPlayRefreshCoordinatorTests` failed on every local `just verify` during the redesign batches and blocked every task session: `AppContainer.fixture` built a real `LocationManager()`, so the outcome depended on the simulator's CoreLocation TCC state, and one injected `sleep` closure served both the location wait and the retry backoff, so a test could not distinguish them | L2 checks: `CarPlayLocationSource` seam with `FixtureLocationManager` in `AppContainerFixture`, split `backoffSleep` / `locationPollSleep` closures, and the first test for REQ-REGION-009 (`requirements/region-model.md`), which RD-8 had left untestable | `CarPlayRefreshCoordinatorTests` inject a fixture location source and separate sleep closures (REQ-REGION-009); no test reads simulator location permission | Point `AppContainer.fixture` back at `LocationManager()` and run `just test` on a simulator that never granted location |
| 2026-09-17 | Every merge to `main` that passed checks archived and uploaded a TestFlight build, documentation commits included; App Store Connect rejected version 3.0.0 build 106 with `ITMS-90382: Upload limit reached` during the redesign batches | L1 `decisions/0012-tag-gated-testflight-builds.md` (supersedes the `testflight` row of ADR 0010); `docs/operations/release-process.md` invariants 3-4 | `tests.yml` promotes nothing; `testflight` moves only through `promote-testflight.sh`, which requires an annotated `tf-MAJOR.MINOR.PATCH-BUILD` tag | Push a commit to `main` and confirm no Xcode Cloud build starts; then tag it and confirm one does |
| 2026-09-16 | Siri answered "All Clear" / "Alert Active" (DriveCheckKit catalog) while phone, CarPlay, widgets, and Live Activity showed "No Alert" / "Alert"; the Siri test matched the raw key, so drift passed | L1 `requirements/surfaces-and-pro-gating.md` (REQ-SURF-001) | `StatusWordingConsistencyTests`; `AlertStatusAnswerBuilderTests` asserts resolved wording | Change one catalog's `All Clear` value and run `just test` |
